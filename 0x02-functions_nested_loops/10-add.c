#include "main.h"

/**
 * add - adds two numbers
 * @a: first parameter
 * @b: second parameter
 * Return: results of addition
 */
int add(int a, int b)
{
	int results;

	results = a + b;
	return (results);
}
