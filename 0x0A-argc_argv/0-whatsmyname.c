#include <stdio.h>

/**
 * main - prints the programe name.
 * @argc: argument count.
 * @argv: argument vector.
 * Return: 0 on success.
 */
int main(int __attribute__ ((unused)) argc, char **argv)
{
	printf("%s\n", argv[0]);
	return (0);
}
