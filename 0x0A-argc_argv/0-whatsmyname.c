#include <stdio.h>

/**
 * main - prints programe name followed by \n
 * @argc: argument count
 * @argv: argument vector
 * Return: Always 0 (on success)
 */
int main(int __attribute__ ((unused)) argc, char **argv)
{
	printf("%s\n", argv[0]);
	return (0);
}
